import { styled } from "styled-components";
import { container } from "../../../GlobalStyle";

export const BannerContainer = styled(container)``;
export const BanneTopLine = styled.div``;
export const BannerItmes = styled.div``;
